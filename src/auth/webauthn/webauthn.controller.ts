/**
 * Los endpoints WebAuthn se consolidaron en AuthController.
 * Este archivo se mantiene vacío para no romper el módulo.
 */
import { Controller } from '@nestjs/common';

@Controller()
export class WebAuthnController {}
